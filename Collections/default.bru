meta {
  name: default
  type: http
  seq: 2
}

get {
  url: {{backend_base_url}}
  body: none
  auth: bearer
}

headers {
  accept: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: false
  timeout: 0
}
